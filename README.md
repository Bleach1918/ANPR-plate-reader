## ANPR Project using YOLO

Its a tkinter GUI that uses OpenCV to deal with video input, passing it to YOLO with a trained model to return x, y coordinates back to OpenCV.
Then, using easyocr it reads and prints out the plate shown in video.
